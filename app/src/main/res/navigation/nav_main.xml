<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_main"
    app:startDestination="@id/mainFragment">

    <fragment
        android:id="@+id/mainFragment"
        android:name="com.serhii.stasiuk.buttontoaction.presentation.fragment.main.MainFragment"
        tools:label="Main"
        tools:layout="@layout/fragment_main">
        <action
            android:id="@+id/action_to_contactsFragment"
            app:destination="@+id/contactsFragment" />
    </fragment>

    <fragment
        android:id="@+id/contactsFragment"
        android:name="com.serhii.stasiuk.buttontoaction.presentation.fragment.contacts.ContactsFragment"
        tools:label="Contacts"
        tools:layout="@layout/fragment_contacts" />
</navigation>